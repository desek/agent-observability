---
description: Install the local observability stack and wire this agent into it, from a fresh clone to verified telemetry, with consent for every change.
---

Read `skills/observability-install/SKILL.md` in this repository and follow it
exactly to install the observability stack and wire this agent into it.

The skill is the single source of the instruction. Do not improvise an
installation of your own. Follow its rules: check the prerequisites, offer to
start the stack and ask first, verify the stack, present a plan and get consent
before any change, configure with a backup and validation, then prove the result
by driving one turn and confirming the metric, the log, and the trace arrive.
Report success only after you have seen the data.
